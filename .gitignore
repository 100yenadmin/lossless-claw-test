.pebbles
# Match both directory and symlink-to-directory forms — local dev checkouts
# sometimes symlink node_modules onto an external volume to keep scratch
# worktrees thin, and a `node_modules/` rule alone does not ignore the
# symlink (git treats the symlink itself as a file).
node_modules
node_modules/
dist/
*.tsbuildinfo
.DS_Store
.worktrees

# TUI
tui/lcm-tui
dist/
tui/tui

# CE planning artifacts (local agent scratch, never commit)
docs/plans/
docs/brainstorms/
.compound-engineering/
TASK.md
BRIEF.md
PROGRESS.md
